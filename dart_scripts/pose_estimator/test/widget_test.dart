import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test placeholder — real tests require camera + TFLite mocks.
    expect(true, isTrue);
  });
}
